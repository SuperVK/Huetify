import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    widgets: {
        top: 25,
        display: 'flex',
        alignItems: 'center',
        alignContent: 'center',
        flexDirection: 'column',
    }
})